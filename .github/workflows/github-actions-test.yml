name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: push

jobs:
  gitHub-actions-demo:
    uses: hiasad/GithubActionsReusable/.github/workflows/hello-reusable-workflow.yml@main